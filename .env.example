# PostgreSQL profile (used by Docker Compose or SPRING_PROFILES_ACTIVE=postgres)
DB_URL=jdbc:postgresql://localhost:5432/service_ops
DB_USERNAME=service_ops
DB_PASSWORD=service_ops

# Optional demo seed data
DEMO_DATA_ENABLED=false
